export enum Environment {
  DEV = 'development',
  LOCAL = 'local',
  STAGING = 'staging',
  PRODUCTION = 'production',
}

export const DEFAULT_PORT = 4000;

export enum Redis {
  DEFAULT_PORT = 6379,
  DEFAULT_HOST = 'localhost',
}

export const RandomKeyLength = 16;
